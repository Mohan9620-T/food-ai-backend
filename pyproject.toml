[tool.ruff]
target-version = "py313"
line-length = 100
extend-exclude = [
    ".venv",
    "app/alembic/versions",
    "food-ai-ui",
    "pytest-cache-files-*",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
